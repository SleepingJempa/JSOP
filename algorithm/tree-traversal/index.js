import Graph from '../../data-structures/graph/index.js'
import Node from '../../data-structures/node/index.js'

class TreeTraversal {
    /**
     * 
     * @param {Function} action 
     */
    constructor(action, flag) {
        this.action = action
        this.flag = flag
    }
}

export default TreeTraversal